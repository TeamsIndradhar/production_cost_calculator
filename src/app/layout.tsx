import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "Premium Cost Calculator | MSME Toolkit",
  description: "Professional cost-per-piece calculator for manufacturers. Calculate production costs, selling prices, and profit margins in real-time.",
  keywords: ["cost calculator", "manufacturing", "MSME", "costing", "profit margin", "selling price"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.variable} font-sans antialiased`}>
        {children}
      </body>
    </html>
  );
}
